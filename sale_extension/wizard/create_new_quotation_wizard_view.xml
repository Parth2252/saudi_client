<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_create_new_quotation_wizard_form" model="ir.ui.view">
            <field name="name">create.new.quotation.wizard.form</field>
            <field name="model">create.new.quotation.wizard</field>
            <field name="arch" type="xml">
                <form string="Create New Quotation">
                    <group>
                        <field name="sale_id" invisible="1"/>
                        <field name="partner_id" domain="[('customer_rank', '>', 0)]"/>
                    </group>
                    <div class="alert alert-warning" role="alert">
                        <field name="warning_message" nolabel="1"/>
                    </div>
                    <footer>
                        <button name="action_create_quotation" string="Create Quotation" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_create_new_quotation_wizard" model="ir.actions.act_window">
            <field name="name">Create New Quotation</field>
            <field name="res_model">create.new.quotation.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
